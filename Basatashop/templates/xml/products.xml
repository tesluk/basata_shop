{% load static %}
<products group_id="{{  }}" group_name="{{  }}" type_id="{{  }}" type_name="{{  }}">
	<product id="{{  }}" name="{{  }}" price="{{  }}">
		<pic>{{  }}</pic>
	</product>	
</products>

<groups>
	{% for group in groups %}
	<group id="{{ group.id }}" name="{{ group.name }}" count="{{ group.count }}">
		<pic>{{ group.picture_l }}</pic>
	</group>
	{% endfor %}
</groups>